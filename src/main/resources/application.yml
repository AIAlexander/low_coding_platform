spring:
  application:
    name: low-coding-platform
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/low_coding_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
  profiles:
    active: local
server:
  port: 8023
  servlet:
    context-path: /api


springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.alex.lowcodingplatform.controller
# knife4j???????????????
knife4j:
  enable: true
  setting:
    language: zh_cn